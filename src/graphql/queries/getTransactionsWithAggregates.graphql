query getTransactionsWithAggregates {
  Transactions_aggregate {
    aggregate {
      sum {
        amount
      }
      count
    }
  }
  Transactions(order_by: {transactionDate: asc}) {
    ...transactions
    Account {
      name 
    }
  }
}